package derived;

import data.IcEngine;

public class Cng extends IcEngine {
    public Cng(int modelNo, int maxPower, int maxRPM, int displacement, int noOfCyllender, String fuelType) {
        super(modelNo, maxPower, maxRPM, displacement, noOfCyllender, fuelType);
    }
}
